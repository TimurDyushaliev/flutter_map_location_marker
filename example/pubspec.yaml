name: location_marker_example
description: Example of FlutterMapLocationMarkerPlugin
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_joystick : ^0.0.2
  cupertino_icons: ^0.1.3
  flutter_map_location_marker:
    path: ../
  geolocator:
    git:
      url: git@github.com:TimurDyushaliev/flutter-geolocator.git
      path: geolocator

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
